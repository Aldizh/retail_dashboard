import { app, items } from './Stitch';
import {
  isLoggedIn, loginAnonymous, loginWithKey, logoutUser,
} from './authentication';

export { app, items };
export {
  isLoggedIn, loginAnonymous, loginWithKey, logoutUser,
};
